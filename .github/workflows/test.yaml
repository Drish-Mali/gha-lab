name: test
on:
  push:
    branches: [ main ]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name : hello word
      - run: echo "Hello world"
        shell: bash
  goodbye:
      runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: python inside.py
